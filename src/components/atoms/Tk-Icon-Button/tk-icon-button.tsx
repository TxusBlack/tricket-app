import React from "react";

const TkIconButton = ({
    size,
    nameIcon
}) => {
    return (
        <button>
            <i>{nameIcon}</i>
        </button>
    )
};

export default TkIconButton;
